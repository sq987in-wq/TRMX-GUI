# python
__pycache__/
*.pyc
.pytest_cache/
*.log

# android / gradle (CI and Android Studio artifacts — never commit)
.gradle/
local.properties
.idea/
*.iml
.kotlin/
captures/
*.jks
*.keystore
# committed on purpose: the stable CI debug-signing test key (public
# passwords, debug builds only — ADR-006); must not be ignored
!android/config/debug.keystore
